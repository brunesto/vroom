{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug vroom-in.json",
            "type": "cppdbg",
            "request": "launch",            
            "program": "${workspaceFolder}/bin/vroom",
            "args": ["-i","${workspaceFolder}/vroom-in.json"],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": false,
            "MIMode": "gdb",
            "preLaunchTask": "make build" // This must match the label in tasks.json
        }
    ]
}